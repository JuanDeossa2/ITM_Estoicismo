server:
  port: 8084

# datos para el ORM JPA, conexión a MySql
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/itm_estoicismo_db
    username: root
    password: admin123
    driverClassName: com.mysql.cj.jdbc.Driver   # <-- 1. CORREGIDO AQUÍ

  jpa:
    open-in-view: false                         # <-- 3. AGREGADO AQUÍ (Nota los espacios)
    hibernate:                                  # <-- (La línea 13 vieja se borró)
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true